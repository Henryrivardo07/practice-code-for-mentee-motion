import { motion } from 'motion/react';
import { useState } from 'react';

export const AnimateWithStatePractice = () => {
  const [isExpanded, setIsExpanded] = useState(false);

  return (
    <div>
      <h1>Animate With State Practice</h1>
    </div>
  );
};
